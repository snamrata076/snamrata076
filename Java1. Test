
class Demo
{
    static string wordaReverse (stringstr)
    {
        int i=str.length()-1;
        int start ,end=i+1;
        string result="";
        
        while  (i>=0)
       {
           if (str.charAt(I)=='')
           {
               star=i+1
               while(start!=end)
               result +=str.charAt(start++)
               
               result +='';
               
               end=i
              }
              i--;
       }
           
       Start=0
       while(start!=end)
       result +=str.charAt(start++);
       
        return result;
    }
    public static void main (string[]args)
    {
        String str="JAVA SKILLS";
        
        system .out.print(wordReverse(str));
    }
    }
